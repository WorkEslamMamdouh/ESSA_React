<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>الصفحة غير موجودة</title>
    <style>
        ._container_Found {
            margin-top: 10%;
            padding: 0;
            font-family: 'Cairo', sans-serif; 
            display: flex;
            justify-content: center;
            align-items: center; 
            color: #333;
            text-align: center;
        }

        .not-found-container {
            max-width: 600px;
            padding: 30px;
            background-color: white;
            box-shadow: 0 16px 13px rgba(0,0,0,0.2);
            border-radius: 20px;
        }

        .not-found-title {
            font-size: 100px;
            font-weight: bold;
            color: #ff6f61;
        }

        .not-found-message {
            font-size: 24px;
            margin-top: -20px;
        }

        .home-link {
            display: inline-block;
            margin-top: 30px;
            padding: 10px 25px;
            background-color: #ff6f61;
            color: white;
            text-decoration: none;
            border-radius: 8px;
            transition: background-color 0.3s;
        }

            .home-link:hover {
                background-color: #e95a4b;
            }

    </style>
</head>
<body>
    <div class="_container_Found">
        <div class="not-found-container">
            <div class="not-found-title">There Not pages...</div>
        </div>
    </div>
</body>
</html>
